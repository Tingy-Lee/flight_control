
## 📋 状态机总览

你的飞控系统定义了 **9个飞行模式**，在 [app_types.h](file:///Users/litianyi/Desktop/flight_control/app/app_types.h#L79-L89) 中定义：

| 模式 | 枚举值 | 说明 |
|------|--------|------|
| LOCKED | 0 | 锁定模式（初始状态） |
| STANDBY | 1 | 待机模式 |
| ARMED_IDLE | 2 | 待命模式（已解锁） |
| TAKEOFF | 3 | 起飞模式 |
| ALT_HOLD | 4 | 定高模式 |
| MANUAL_STAB | 5 | 手动增稳模式（预留） |
| MISSION | 6 | 任务模式（预留） |
| LAND | 7 | 降落模式 |
| FAILSAFE | 8 | 故障保护模式 |

---

## 🎮 遥控器配置

你使用的是 **FlySky iBUS** 协议（FS-i6X + FS-iA10B），通道映射在 [app_config.h](file:///Users/litianyi/Desktop/flight_control/app/app_config.h#L64-L75) 中：

| 物理通道 | 代码索引 | 功能 |
|----------|----------|------|
| CH1 | 0 | 横滚（Roll） |
| CH2 | 1 | 俯仰（Pitch） |
| CH3 | 2 | 悬停高度调整（Hover Height） |
| CH4 | 3 | 航向（Yaw） |
| CH5 | 4 | 油门（Throttle） |
| CH7 | 6 | 解锁开关（Arm Switch） |

**关键配置：**
- 解锁开关阈值：**1600μs**（`FC_RC_ARM_THRESHOLD_US`）
- 解锁时最大油门：**1030μs**（`FC_ARM_THROTTLE_MAX_US`）
- 起飞触发油门：**约1650μs**（`FC_TAKEOFF_TRIGGER_US`）

---

## 🔄 状态转换图

```
    LOCKED
       │
       ▼
   STANDBY ◄──────────────────────────────────┐
       │                                     │
       │ (解锁开关+低油门+安全姿态)          │
       ▼                                     │
  ARMED_IDLE ◄──────────┐                    │
       │                │                    │
       │ (推油门起飞)    │                    │
       ▼                │                    │
    TAKEOFF             │                    │
       │                │                    │
       │ (到达目标高度)  │                    │
       ▼                │                    │
   ALT_HOLD             │                    │
       │                │                    │
       │ (关闭解锁开关)  │                    │
       ▼                │                    │
     LAND ──────────────┘                    │
       │                                     │
       │ (降落完成)                          │
       └─────────────────────────────────────┘
       
   FAILSAFE (任何错误触发)
       │
       ▼ (关闭解锁开关)
    LOCKED
```

---

## 🎯 详细状态转换说明

### 1️⃣ **LOCKED → STANDBY**
- **触发条件**：系统启动自动进入，无需操作（[commander_task.c](file:///Users/litianyi/Desktop/flight_control/tasks/Src/commander_task.c#L163-L166)）
- **状态**：`armed = false`

---

### 2️⃣ **STANDBY → ARMED_IDLE（解锁）**
**遥控器操作（同时满足）：**
1. ✅ **CH7（解锁开关）** 拨到 **高位**（>1600μs）
2. ✅ **CH5（油门）** 保持 **低位**（≤1030μs）
3. ✅ **姿态安全**：横滚/俯仰角 ≤ 20°（0.35rad）
4. ✅ **高度计有效**

代码位置：[commander_task.c](file:///Users/litianyi/Desktop/flight_control/tasks/Src/commander_task.c#L168-L175)

**进入此模式后：**
- `armed = true`
- 记录起飞点高度作为 `home_altitude_m`

---

### 3️⃣ **ARMED_IDLE → TAKEOFF（起飞）**
**遥控器操作：**
- ✅ **CH7（解锁开关）** 保持高位
- ✅ **CH5（油门）** 推到 **高位**（≥1650μs）
- ✅ 高度计有效

代码位置：[commander_task.c](file:///Users/litianyi/Desktop/flight_control/tasks/Src/commander_task.c#L177-L183)

**起飞参数：**
- 目标高度：home高度 + **0.5m**
- 爬升速率：**0.5m/s**

---

### 4️⃣ **TAKEOFF → ALT_HOLD（进入定高）**
**自动触发（无需操作）：**
- 当实际高度 ≥ 目标高度 - 0.1m 时自动切换

代码位置：[commander_task.c](file:///Users/litianyi/Desktop/flight_control/tasks/Src/commander_task.c#L185-L191)

---

### 5️⃣ **ALT_HOLD / TAKEOFF → LAND（降落）**
**遥控器操作：**
- ❌ **CH7（解锁开关）** 拨回 **低位**

代码位置：[commander_task.c](file:///Users/litianyi/Desktop/flight_control/tasks/Src/commander_task.c#L193-L198)

**降落参数：**
- 下降速率：**0.35m/s**
- 降落完成条件：高度 ≤ home高度 + 0.08m **且** 垂直速度 ≤ 0.15m/s **且** 稳定1秒

---

### 6️⃣ **LAND → STANDBY（回到待机）**
**自动触发（无需操作）：**
- 降落完成检测通过后自动切换

代码位置：[commander_task.c](file:///Users/litianyi/Desktop/flight_control/tasks/Src/commander_task.c#L200-L204)

---

### 7️⃣ **任何模式 → FAILSAFE（故障保护）**
**触发条件（任一）：**
- ❌ IMU不健康
- ❌ 气压计不健康
- ❌ 遥控器信号丢失/故障保护
- ❌ （当前 `FC_ALLOW_ARMING = 0`，禁止解锁）

代码位置：[safety.c](file:///Users/litianyi/Desktop/flight_control/modules/safety/safety.c#L8-L33)

---

## 🎮 遥控器控制摇杆功能（定高模式下）

在 `ALT_HOLD` 模式下，各摇杆的作用（[decision_task.c](file:///Users/litianyi/Desktop/flight_control/tasks/Src/decision_task.c#L77-L94)）：

| 摇杆 | 功能 | 范围 |
|------|------|------|
| CH1（Roll）| 横滚角目标 | ±20° |
| CH2（Pitch）| 俯仰角目标 | ±20° |
| CH3（Hover Height）| 悬停高度调整率 | ±1.0m/s（积分到±2m） |
| CH4（Yaw）| 航向角速度目标 | ±120°/s |

---

## ⚠️ 重要提示

当前 `FC_ALLOW_ARMING = 0`（[app_config.h](file:///Users/litianyi/Desktop/flight_control/app/app_config.h#L12)），**解锁功能被禁用**！你需要将其改为 `1` 才能实际飞行。

---

## 📊 总结

你的状态机设计非常清晰且安全，遵循了典型的无人机飞行逻辑：锁定→待机→解锁→起飞→定高→降落→待机的完整闭环。所有状态转换都有明确的安全检查，确保飞行安全！
